conda create --name dexterous_grasp python=3.10
conda activate dexterous_grasp

# for understanding module
pip install requests
sudo apt-get install portaudio19-dev
pip install pyaudio
pip install pygame

# for awake
cd external
git clone https://github.com/Kitt-AI/snowboy.git snowboy
sudo apt update
sudo apt install swig
sudo apt install libblas-dev liblapack-dev
sudo apt install libatlas-base-dev
cd snowboy/swig/Python3
make


# for vision module
cf external
git clone https://github.com/IDEA-Research/Grounded-SAM-2.git gsam2
cd gsam2
cd checkpoints
bash download_ckpts.sh #为了实现更好的sam效果，请将sh文件里面47-54的代码注释，只保留sam large的这部分
cd ..
cd gdino_checkpoints
bash download_ckpts.sh #为了实现更好的dino效果，请将sh文件里面18-19行代码注释，只保留swin-b的这部分
pip3 install torch torchvision torchaudio
cd ..
pip install -e .

wget https://developer.download.nvidia.com/compute/cuda/12.1.0/local_installers/cuda_12.1.0_530.30.02_linux.run
sudo sh cuda_12.1.0_530.30.02_linux.run
# 添加 CUDA 12.1 的路径
export PATH=/usr/local/cuda-12.1/bin${PATH:+:${PATH}}
export LD_LIBRARY_PATH=/usr/local/cuda-12.1/lib64${LD_LIBRARY_PATH:+:${LD_LIBRARY_PATH}}

pip install --no-build-isolation -e grounding_dino

pip install opencv-python
pip install supervision
pip install pycocotools
pip install transformers
pip install addict
pip install yapf
pip install timm

export HF_ENDPOINT=https://hf-mirror.com

# for planning module
pip install numpy

# for control module
pip install serial

# New update 12.11

## for clip
pip install git+https://github.com/openai/CLIP.git

## for dinox
pip install dds_cloudapi_sdk